<link rel="preconnect" href="https://www.googletagmanager.com" />
<link rel="preconnect" href="https://www.google-analytics.com" />
<script is:inline defer>
  (function () {
    if (
      window.location.origin !== "https://www.brazilmentioned.com" && window.location.origin !== 'https://brazilmentioned.com' ||
      window.location.search.match(/trd3=false/)
    ) {
      console.log("Google Analytics: disabled");
      return;
    }

    window.dataLayer = window.dataLayer || [];

    function gtag() {
      dataLayer.push(arguments);
    }

    gtag("js", new Date());
    gtag("config", "G-KL6KDZ27W3");

    function load() {
      let script = document.createElement("script");
      script.async = true;
      script.src = "https://www.googletagmanager.com/gtag/js?id=G-KL6KDZ27W3";
      document.head.appendChild(script);
    }

    if (window.innerWidth >= 1024) {
      load();
    } else {
      setTimeout(function () {
        load();
      }, 5000);
    }
  })();
</script>
